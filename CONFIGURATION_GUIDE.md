# 🎯 UUMI 网站配置指南

> 本指南帮助您快速了解项目的关键配置，特别是品牌标识（Logo/Favicon）的配置方法，方便您在未来项目中快速复用。

---

## 📌 浏览器标签栏 Logo（Favicon）配置

### ❓ 浏览器标签栏显示的是哪个文件？

**答案：`/public/uumi-logo.png`** ⭐

当您在浏览器中打开网站时，标签栏显示的小图标（favicon）就是这个文件。

### 🔧 配置位置

**文件：`src/app/layout.tsx`**

```typescript
export const metadata: Metadata = {
  title: "UUMI 悠悠米科技 - 让智能带来全新视界",
  description: "...",
  
  // 👇 这里配置 favicon
  icons: {
    icon: "/uumi-logo.png",           // 标准浏览器图标（标签栏显示）
    shortcut: "/uumi-logo.png",       // 快捷方式图标
    apple: "/uumi-logo.png",          // Apple 设备（iOS/macOS）图标
  },
  
  openGraph: {
    // ...
  },
};
```

### 📝 新项目中如何替换 Favicon？

**步骤 1：准备您的 Logo 文件**
- 文件格式：PNG、ICO、SVG 都可以
- 建议尺寸：至少 180×180px（正方形）
- 文件名：任意，例如 `my-logo.png`
- 位置：放在 `public/` 目录下

**步骤 2：修改配置文件**
打开 `src/app/layout.tsx`，找到 `metadata` 中的 `icons` 部分：

```typescript
icons: {
  icon: "/my-logo.png",        // 👈 改成您的文件名
  shortcut: "/my-logo.png",    // 👈 改成您的文件名
  apple: "/my-logo.png",       // 👈 改成您的文件名
},
```

**步骤 3：清除浏览器缓存**
- Chrome: Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
- 或者使用无痕模式查看效果

### 🎨 不同设备的图标建议

| 设备/场景 | 配置项 | 推荐尺寸 | 格式 |
|----------|--------|---------|------|
| 浏览器标签栏 | `icon` | 32×32px 或更大 | PNG/ICO |
| 桌面快捷方式 | `shortcut` | 192×192px | PNG |
| iOS/macOS | `apple` | 180×180px | PNG |
| Android | `icon` | 192×192px | PNG |

**最简单的方案**：使用一张 512×512px 的正方形 PNG 图片，浏览器会自动缩放。

---

## 🎨 Logo 配置（网站页面中显示的 Logo）

### 📍 Logo 在哪里使用？

1. **导航栏 Logo**（页面顶部）
2. **页脚 Logo**（页面底部）
3. **Favicon**（浏览器标签栏）

### 📝 如何替换页面中的 Logo？

**当前配置：**
- Logo 文件：`/public/uumi-logo.png`
- 配置文件：`src/app/page.tsx`

**查找和替换 Logo：**

```bash
# 在项目中搜索 logo 引用
grep -r "uumi-logo.png" src/
```

**主要位置（`src/app/page.tsx`）：**

```typescript
// 导航栏 Logo（第 87 行左右）
const UUMILogo = memo(({ className = "w-12 h-12" }: { className?: string }) => (
  <div className={className}>
    <Image
      src="/uumi-logo.png"        // 👈 这里
      alt="UUMI Logo"
      width={48}
      height={48}
      className="w-full h-full object-contain"
      priority
    />
  </div>
));

// 页脚 Logo（第 678 行左右）
<Image
  src="/uumi-logo.png"            // 👈 这里
  alt="UUMI"
  width={40}
  height={40}
  className="object-contain"
/>
```

**替换方法：**
1. 将您的新 Logo 文件放到 `public/` 目录
2. 全局搜索 `"/uumi-logo.png"` 并替换为您的文件名
3. 或者直接用新 Logo 覆盖 `public/uumi-logo.png`（最简单）

---

## 📁 项目结构说明

```
uumi-ai-site/
├── public/                          # 📂 静态资源目录
│   ├── uumi-logo.png               # ⭐ 主 Logo（favicon + 页面显示）
│   └── images/                     # 📂 图片资源
│       ├── README.md               # 图片使用说明
│       ├── case-*.webp             # 案例展示图片
│       └── team/                   # 团队成员头像
│           ├── lichumei.jpg
│           ├── zengdan.jpg
│           ├── wushaonan.jpg
│           └── hehongjian.jpg
│
├── src/                            # 📂 源代码目录
│   ├── app/                        # 📂 Next.js App Router
│   │   ├── layout.tsx              # ⭐ 全局布局 + Favicon 配置
│   │   ├── page.tsx                # ⭐ 主页面 + Logo 引用
│   │   └── globals.css             # 全局样式（颜色、动画）
│   │
│   ├── components/                 # 📂 可复用组件
│   │   └── ui/                     # UI 组件库
│   │
│   ├── data/                       # 📂 数据文件
│   │   └── companyData.json        # ⭐ 网站所有内容数据
│   │
│   └── lib/                        # 📂 工具函数
│       └── utils.ts
│
├── package.json                    # 📦 项目配置 + 依赖
├── next.config.ts                  # ⚙️ Next.js 配置
├── tailwind.config.ts              # 🎨 Tailwind CSS 配置
├── tsconfig.json                   # 🔧 TypeScript 配置
├── README.md                       # 📖 项目说明文档
└── CONFIGURATION_GUIDE.md          # 📖 配置指南（本文件）⭐
```

---

## 🎯 关键配置文件速查

### 1️⃣ 品牌标识（Logo/Favicon）

| 配置项 | 文件位置 | 说明 |
|-------|---------|------|
| Favicon | `src/app/layout.tsx` → `metadata.icons` | 浏览器标签栏图标 |
| 导航栏 Logo | `src/app/page.tsx` → `UUMILogo` 组件 | 页面顶部 Logo |
| 页脚 Logo | `src/app/page.tsx` → Footer 部分 | 页面底部 Logo |
| Logo 文件 | `public/uumi-logo.png` | 实际图片文件 |

### 2️⃣ 网站信息（SEO/元数据）

**文件：`src/app/layout.tsx`**

```typescript
export const metadata: Metadata = {
  title: "UUMI 悠悠米科技 - 让智能带来全新视界",      // 👈 浏览器标题
  description: "...",                                  // 👈 网站描述（SEO）
  keywords: ["AI", "人工智能", ...],                  // 👈 关键词（SEO）
  authors: [{ name: "UUMI Technology" }],             // 👈 作者信息
  
  openGraph: {                                         // 👈 社交媒体分享卡片
    title: "...",
    description: "...",
    url: "https://www.uumi.net",                      // 👈 网站地址
    siteName: "UUMI 悠悠米科技",
  },
};
```

### 3️⃣ 网站内容（文字/数据）

**文件：`src/data/companyData.json`**

这个 JSON 文件包含了网站的所有文字内容：
- 公司信息
- 关于我们
- 核心优势
- 焦点领域
- 案例研究
- 团队介绍
- 联系方式

**修改内容**：直接编辑这个 JSON 文件即可，无需改代码。

### 4️⃣ 颜色主题

**文件：`src/app/globals.css`**

```css
:root {
  --primary: #8B5CF6;       /* 主色：优雅的紫色 */
  --secondary: #EC4899;     /* 次色：温暖的粉色 */
  --accent: #F59E0B;        /* 点缀色：金色 */
  /* ... */
}
```

### 5️⃣ 字体配置

**文件：`src/app/layout.tsx`**

```typescript
const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
  preload: true,
});
```

---

## 📋 新项目快速上手清单

当您基于此项目创建新网站时，按以下顺序修改：

### ✅ 第一步：品牌标识
- [ ] 准备新 Logo 文件（建议 512×512px PNG）
- [ ] 放到 `public/` 目录
- [ ] 修改 `src/app/layout.tsx` 中的 `icons` 配置
- [ ] 修改 `src/app/page.tsx` 中的 Logo 引用（或直接覆盖文件）

### ✅ 第二步：网站信息
- [ ] 修改 `src/app/layout.tsx` 中的 `metadata`
  - title（标题）
  - description（描述）
  - keywords（关键词）
  - url（网址）
- [ ] 修改 `package.json` 中的 `name`、`description`

### ✅ 第三步：内容数据
- [ ] 复制 `src/data/companyData.json`
- [ ] 修改所有公司信息、产品、团队等内容
- [ ] 确保 JSON 格式正确（使用标准双引号 `""`）

### ✅ 第四步：颜色主题（可选）
- [ ] 修改 `src/app/globals.css` 中的 `:root` 颜色变量
- [ ] 调整为您的品牌色

### ✅ 第五步：图片资源
- [ ] 替换 `public/images/case-*.webp`（案例图片）
- [ ] 替换 `public/images/team/*.jpg`（团队头像）
- [ ] 参考 `public/images/README.md` 中的尺寸建议

### ✅ 第六步：Git 和部署
- [ ] 初始化新的 Git 仓库：`git init`
- [ ] 提交代码：`git add . && git commit -m "Initial commit"`
- [ ] 创建 GitHub 仓库并推送
- [ ] 在 Vercel 中导入项目并部署

---

## 🚀 常用命令

```bash
# 开发模式（本地预览）
npm run dev                # 访问 http://localhost:3000

# 生产构建（部署前测试）
npm run build
npm run start              # 访问 http://localhost:3000

# 代码检查
npm run lint

# 搜索 Logo 引用
grep -r "uumi-logo" src/

# 搜索文字内容
grep -r "悠悠米" src/

# 查看 Git 历史
git log --oneline
```

---

## 💡 常见问题

### Q: Favicon 不更新怎么办？
**A:** 浏览器会缓存 favicon，尝试：
1. 硬刷新：Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
2. 清除浏览器缓存
3. 使用无痕模式测试
4. 等待 Vercel 部署完成（1-3 分钟）

### Q: 修改了 JSON 文件为什么没生效？
**A:** 
1. 检查 JSON 格式是否正确（使用 [JSONLint](https://jsonlint.com/) 验证）
2. 重启开发服务器：停止并重新运行 `npm run dev`
3. 清除浏览器缓存

### Q: 修改颜色后样式不对？
**A:**
1. 确保修改的是 `globals.css` 中的 CSS 变量
2. 检查 Tailwind 类名是否正确（如 `text-primary`）
3. 重启开发服务器

### Q: 如何添加新页面？
**A:** 
在 `src/app/` 下创建新文件夹，例如：
```
src/app/about/page.tsx      # 访问 /about
src/app/contact/page.tsx    # 访问 /contact
```

---

## 📞 技术栈说明

| 技术 | 版本 | 用途 |
|-----|------|------|
| Next.js | 15.3.5 | React 框架（App Router） |
| React | 19.0.0 | UI 库 |
| TypeScript | 5.x | 类型安全 |
| Tailwind CSS | 4.x | CSS 框架 |
| Framer Motion | 12.x | 动画库 |

---

## 🎉 总结

**记住这两个核心文件：**

1. **`src/app/layout.tsx`** - 全局配置（Favicon、SEO、字体）⭐
2. **`public/uumi-logo.png`** - 主 Logo 文件 ⭐

**一句话记忆：**
> 浏览器标签栏的图标（favicon）配置在 `src/app/layout.tsx` 的 `metadata.icons` 中，指向 `public/uumi-logo.png` 文件。

---

📖 **相关文档：**
- [Next.js Metadata 文档](https://nextjs.org/docs/app/api-reference/functions/generate-metadata)
- [项目主 README](./README.md)
- [图片资源说明](./public/images/README.md)

🚀 **祝您的新项目顺利！**

